// Online C compiler to run C program online
#include <stdio.h>
#include <string.h>

int main() {
   char a[]="shahan";
   int l=0;
   while(a[l]!='\0'){
       l++;
   }
   char b[l];
   int i;
   for(i=0;i<l;i++){
      b[i]=a[i];
   }
   printf("%s",b);
   
       return 0;
}
       
   
  
