// Online C compiler to run C program online
#include <stdio.h>
#include <string.h>

int main() {
   char a[20];
   printf("enter the word\n");
   fgets(a,sizeof(a),stdin);
   printf("%s",a);
   
   
       return 0;
}
